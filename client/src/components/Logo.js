import React from "react";
import logo from '../logo.png';

const Logo = () => (
	<div className="container">
		<div className="row">
			<img src={logo} alt="Truax Trumpets Logo" className="col s2 offset-s9 amber-text" />
		</div>
	</div>
);

export default Logo;
